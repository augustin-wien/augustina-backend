// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/augustin-wien/augustina-backend/ent/location"
	"github.com/augustin-wien/augustina-backend/ent/workingtime"
)

// WorkingTime is the model entity for the WorkingTime schema.
type WorkingTime struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Day holds the value of the "day" field.
	Day workingtime.Day `json:"day,omitempty"`
	// OpenTime holds the value of the "open_time" field.
	OpenTime string `json:"open_time,omitempty"`
	// CloseTime holds the value of the "close_time" field.
	CloseTime string `json:"close_time,omitempty"`
	// Closed holds the value of the "closed" field.
	Closed bool `json:"closed,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the WorkingTimeQuery when eager-loading is set.
	Edges                  WorkingTimeEdges `json:"edges"`
	location_working_times *int
	selectValues           sql.SelectValues
}

// WorkingTimeEdges holds the relations/edges for other nodes in the graph.
type WorkingTimeEdges struct {
	// Location holds the value of the location edge.
	Location *Location `json:"location,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// LocationOrErr returns the Location value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e WorkingTimeEdges) LocationOrErr() (*Location, error) {
	if e.Location != nil {
		return e.Location, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: location.Label}
	}
	return nil, &NotLoadedError{edge: "location"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*WorkingTime) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case workingtime.FieldClosed:
			values[i] = new(sql.NullBool)
		case workingtime.FieldID:
			values[i] = new(sql.NullInt64)
		case workingtime.FieldDay, workingtime.FieldOpenTime, workingtime.FieldCloseTime:
			values[i] = new(sql.NullString)
		case workingtime.ForeignKeys[0]: // location_working_times
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the WorkingTime fields.
func (wt *WorkingTime) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case workingtime.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			wt.ID = int(value.Int64)
		case workingtime.FieldDay:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field day", values[i])
			} else if value.Valid {
				wt.Day = workingtime.Day(value.String)
			}
		case workingtime.FieldOpenTime:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field open_time", values[i])
			} else if value.Valid {
				wt.OpenTime = value.String
			}
		case workingtime.FieldCloseTime:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field close_time", values[i])
			} else if value.Valid {
				wt.CloseTime = value.String
			}
		case workingtime.FieldClosed:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field closed", values[i])
			} else if value.Valid {
				wt.Closed = value.Bool
			}
		case workingtime.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field location_working_times", value)
			} else if value.Valid {
				wt.location_working_times = new(int)
				*wt.location_working_times = int(value.Int64)
			}
		default:
			wt.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the WorkingTime.
// This includes values selected through modifiers, order, etc.
func (wt *WorkingTime) Value(name string) (ent.Value, error) {
	return wt.selectValues.Get(name)
}

// QueryLocation queries the "location" edge of the WorkingTime entity.
func (wt *WorkingTime) QueryLocation() *LocationQuery {
	return NewWorkingTimeClient(wt.config).QueryLocation(wt)
}

// Update returns a builder for updating this WorkingTime.
// Note that you need to call WorkingTime.Unwrap() before calling this method if this WorkingTime
// was returned from a transaction, and the transaction was committed or rolled back.
func (wt *WorkingTime) Update() *WorkingTimeUpdateOne {
	return NewWorkingTimeClient(wt.config).UpdateOne(wt)
}

// Unwrap unwraps the WorkingTime entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (wt *WorkingTime) Unwrap() *WorkingTime {
	_tx, ok := wt.config.driver.(*txDriver)
	if !ok {
		panic("ent: WorkingTime is not a transactional entity")
	}
	wt.config.driver = _tx.drv
	return wt
}

// String implements the fmt.Stringer.
func (wt *WorkingTime) String() string {
	var builder strings.Builder
	builder.WriteString("WorkingTime(")
	builder.WriteString(fmt.Sprintf("id=%v, ", wt.ID))
	builder.WriteString("day=")
	builder.WriteString(fmt.Sprintf("%v", wt.Day))
	builder.WriteString(", ")
	builder.WriteString("open_time=")
	builder.WriteString(wt.OpenTime)
	builder.WriteString(", ")
	builder.WriteString("close_time=")
	builder.WriteString(wt.CloseTime)
	builder.WriteString(", ")
	builder.WriteString("closed=")
	builder.WriteString(fmt.Sprintf("%v", wt.Closed))
	builder.WriteByte(')')
	return builder.String()
}

// WorkingTimes is a parsable slice of WorkingTime.
type WorkingTimes []*WorkingTime
